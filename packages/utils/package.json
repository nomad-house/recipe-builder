{
  "name": "@codeified/utils",
  "version": "0.0.1",
  "description": "codeified-utils",
  "scripts": {
    "node": "node",
    "ts": "npm run node -- -r ts-node/register",
    "build": "rm -rf dist && tsc"
  },
  "license": "MIT",
  "devDependencies": {
    "ts-node": "latest",
    "typescript": "latest"
  }
}
