{
  "name": "@codeified/config",
  "version": "0.0.1",
  "scripts": {
    "node": "node",
    "ts": "npm run node -- -r ts-node/register",
    "build": "rm -rf dist && tsc"
  },
  "license": "MIT",
  "description": "CODEified",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "private": true
}
