{
  "name": "codeified-backend",
  "version": "0.0.1",
  "description": "codeified-backend",
  "scripts": {
    "node": "node",
    "ts": "npm run node -- -r ts-node/register",
    "neo4j:start": "~/.neo4j/bin/neo4j start",
    "neo4j:stop": "~/.neo4j/bin/neo4j stop",
    "start": "npm run ts bin/devServer",
    "build": "rm -rf dist && tsc",
    "build:watch": "npm run build -- --watch"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.76",
    "convert-lambda-to-express": "^1.1.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "slate": "^0.70.0"
  },
  "private": true
}
